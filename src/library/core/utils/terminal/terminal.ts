/**
 * Terminal functionality.
 * TODO: really read into this kind of thing first then work it out
 */
export default class Terminal {
    /**
     * The amount of lines that have been outputted in the terminal
     */
    public static lines = 0
}